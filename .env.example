# Docker Compose 环境变量配置示例
# 复制此文件为 .env 并根据需要修改值

# 前端暴露端口（映射到宿主机的端口）
# 默认: 5173
# 可选: 任意未被占用的端口，如 3000, 8080, 9000 等
EXPOSE_PORT=5173

# 如果需要修改端口，只需修改上面的值并重新启动：
# docker-compose down
# docker-compose up -d
